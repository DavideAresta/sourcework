{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The shell talks to the backend over HTTP; the page calls no Tauri commands, so this grants nothing beyond the window it lives in.",
  "windows": ["main"],
  "permissions": []
}
